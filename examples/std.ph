
#####
# Standard library
# Most of things used will probably come from here
#####

# dup :: [a]-> [a]
# Put duplicate of value onto stack
{
    $v =
    v v
} $dup =

# pop :: [a] -> []
# Delete top of stack
{
    $_ =
} $pop =

