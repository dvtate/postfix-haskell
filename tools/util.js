

/**
 *
 * @param {string} file - file name/path
 * @param {number} pos - character index in file
 */
function printFileLinePos(file, pos) {
    return {
        line,
        lineNumber, lineOffset,
    }
}