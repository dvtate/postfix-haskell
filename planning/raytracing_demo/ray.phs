"lang" require use

"./vec3.phs" require $Vec3 =

(
    Vec3.Vec3   # origin
    Vec3.Vec3   # direction
) class $Ray =
((Ray): unpack ( $orig $dir ) = orig ) $origin =
((Ray): unpack ( $orig $dir ) = dir )  $direction =

(( Ray F64 ):  $distance =
    unpack ( $origin $direction ) =
    direction distance * origin +
) $get_point =