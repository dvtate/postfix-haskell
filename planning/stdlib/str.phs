"./prelude.phs" include use

# String TypeClass
{ I32 I32 } pack class $Str =
{ type Str == } { unpack pop } $.len fun
{ type Str == } { unpack swap pop } $.addr fun


{ # TODO convert make into overrideable function ?
    { { $lit $type } =
        type Str ==
        { lit } pack { I32 I32 } pack ==
            &&
    }
    {
        pop
        $lit = { lit } pack Str make
    } $make fun
} pop


{ $lit =
    { lit } pack
    Str make
} $to_Str =
